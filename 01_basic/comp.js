// in comparison always check the data type as well
// so you'll get the desired output
//  console.table([undefined==0,undefined>=0,undefined<=0]);
//  console.table([null>=0])

// ------ don't use the above



 // === (strict check {value + data types})
 console.log('2'===2);

 